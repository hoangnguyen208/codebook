# Auth Credentials - Email/Password Provider

## Overview
Add Credentials provider for username/password authentication with registration via Duende Identity Server + Next Authentication.

## Requirements
- Create registration API route at `/api/auth/register`
- Routing to Duende Identity Server to register a new user
- After successfully register, redirecting to /dashboard page
